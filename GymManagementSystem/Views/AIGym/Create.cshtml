@model GymManagementSystem.Models.AIGymProfile

@{
    ViewData["Title"] = "Yapay Zeka Planlayıcı";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <h3 class="card-title mb-0">🏋️‍♂️ AI Fitness Planlayıcı</h3>
                </div>
                <div class="card-body">
                    <p class="text-muted">Size özel bir plan oluşturmak için bilgilerinizi girin.</p>
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Weight" class="form-label"></label>
                            <input asp-for="Weight" class="form-control" placeholder="70" />
                            <span asp-validation-for="Weight" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Height" class="form-label"></label>
                            <input asp-for="Height" class="form-control" placeholder="175" />
                            <span asp-validation-for="Height" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Goal" class="form-label"></label>
                            <select asp-for="Goal" class="form-select">
                                <option value="">-- Hedef Seçiniz --</option>
                                <option value="Lose Weight">Kilo Vermek</option>
                                <option value="Build Muscle">Kas Yapmak</option>
                                <option value="Fitness & Health">Sağlık ve Fitlik</option>
                            </select>
                            <span asp-validation-for="Goal" class="text-danger"></span>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                ✨ Planı Oluştur
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}